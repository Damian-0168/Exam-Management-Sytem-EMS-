<analysis>**original_problem_statement:**
The user wants to update an existing School Examination & Academic Management System (SEAMS) based on a detailed Software Requirements Specification (SRS) document. The initial codebase has a React/TypeScript frontend integrated with Supabase and a minimal FastAPI backend.

The user's requirements, evolving through the conversation, are:
1.  **Architecture:** Continue using Supabase for PostgreSQL and Authentication, but build out the FastAPI backend for complex business logic (e.g., secure file URL generation, audit logging). Use Supabase Storage for file uploads.
2.  **Phase 1 - Security & Access Control (Highest Priority):**
    *   Implement Role-Based Access Control (RBAC) for different user roles (Admin, Teacher).
    *   Secure PDF uploads by making the storage bucket private and using signed URLs.
    *   Add watermarking to PDF previews.
    *   Create an  system to track critical user actions.
3.  **Admin Account & UI:**
    *   Create a robust School Admin role with full access to all data within their assigned school.
    *   Fix all bugs related to the Admin and Teacher signup/onboarding flow.
    *   Redesign the UI cards for Exams, Tests, and Practicals to be more modern and informative.
4.  **Future Phases:** Implement PDF versioning, bulk student import, advanced analytics, and a notification system (using SendGrid).

**User's preferred language**: English

**what currently exists?**
- A full-stack application with a React/Vite frontend and a FastAPI backend.
- Supabase is used for the PostgreSQL database, authentication, and file storage.
- The frontend contains UI for managing students, exams, and scores, but is plagued by several critical bugs in the authentication and user setup flows.
- The backend has been scaffolded with a proper structure (, , ) and includes placeholder endpoints for audit logging, permissions, and generating signed URLs.
- Several database migration files have been created to add new tables (, , etc.) and columns ( to ), but they have **not been applied** to the database, causing schema mismatch errors.
- Multiple attempts have been made to fix the Admin/Teacher signup flow, including creating new components (, , ) and refactoring existing ones ().

**Last working item**:
- **Last item agent was working:** Fixing a critical user already registered error that occurs during Admin account creation. The root cause was identified: the  component was incorrectly trying to create a new Supabase auth user instead of finding an existing user by email and promoting their role to 'admin'.
- **Status:** IN PROGRESS
- **Agent Testing Done:** N
- **Which testing method agent to use?** manual testing by curl
- **User Testing Done:** N

**All Pending/In progress Issue list**:
- **Issue 1 (P0):** user already registered error during Admin account creation.
- **Issue 2 (P1):** Database migrations are unapplied, causing schema mismatch errors like 'role column not found'.
- **Issue 3 (P2):** The Admin/Teacher signup and onboarding flow is unstable and not fully verified.
- **Issue 4 (P3):** The backend API and frontend are not fully integrated; the frontend still heavily relies on direct Supabase client calls.

**Issues Detail:**
- **Issue 1: user already registered error during Admin account creation**
    - **Attempted fixes:** The agent correctly identified the logical flaw and overwrote  with a new version intended to promote an existing user by updating their role, rather than re-registering them.
    - **Next debug checklist:**
        1.  Verify the logic in  correctly checks for an existing user and calls an appropriate function to update their role in the  table.
        2.  Ensure the  table has an  column to look up the user. The agent noted this was a necessary next step but didn't implement it. A migration is needed.
        3.  Test the two flows: a) creating a brand new admin user, and b) promoting an existing teacher to an admin.
    - **Why fix this issue and what will be achieved with the fix?** This is a critical blocker preventing the creation of Admin users, which is a core requirement of Phase 1. Fixing it will unblock all other administrative features.
    - **Status:** IN PROGRESS
    - **Is recurring issue?** Y
    - **Should Test frontend/backend/both after fix?** both
- **Issue 2: Database migrations are unapplied**
    - **Attempted fixes:** The agent created several SQL migration files (, etc.) and a guide (). However, the agent never executed the steps to apply these migrations.
    - **Next debug checklist:**
        1.  Locate the final, correct migration file: .
        2.  Follow the instructions in  to apply the migration to the Supabase database. This will likely involve using the Supabase CLI or executing the SQL directly in the Supabase dashboard's SQL Editor.
        3.  After applying, restart the backend and frontend services.
    - **Why fix this issue and what will be achieved with the fix?** This will resolve all schema-related errors (e.g., role column not found) and synchronize the database with the application code, allowing RBAC and other features to function.
    - **Status:** NOT STARTED
    - **Is recurring issue?** Y
    - **Should Test frontend/backend/both after fix?** both

**In progress Task List**:
- **Task 1: Stabilize User Authentication and Onboarding (Phase 1)**
    - **Where to resume:** Resume by fixing the user already registered error (Issue 1) and applying the database migrations (Issue 2).
    - **What will be achieved with this?** A stable system where both Teacher and Admin accounts can be created and onboarded without errors, with correct roles and permissions assigned.
    - **Status:** IN PROGRESS
    - **Should Test frontend/backend/both after fix?** both
    - **Blocked on something:** Blocked by unresolved Issues 1 & 2.

**Upcoming and Future Tasks**
- **Upcoming Tasks (P0):**
    - Integrate the redesigned  into  and other relevant views.
    - Implement and test the PDF watermarking using  via .
    - Fully implement the Audit Logging system by connecting the frontend logger () to the backend API ().
    - Make the Supabase  storage bucket private and integrate the  backend endpoint into the frontend's .
- **Future Tasks (P1):**
    - **Phase 2:**
        - PDF Version History ( table).
        - Bulk Student Import (CSV/Excel).
        - System Configuration UI ( table).
    - **Phase 3:** Advanced Analytics & Reporting.
    - **Phase 4:** Notification System (SendGrid).
    - **Phase 5:** Question Bank & 2FA.

**Completed work in this session**
- **Initial Setup:** Mandatory , , and  updates completed.
- **Backend Scaffolding:** Created a structured FastAPI backend with routers and placeholder endpoints for Phase 1 features.
- **Frontend Component Creation:**
    - , : Auth flow components (currently buggy).
    - : A reusable dropdown for selecting a school.
    - , : For post-signup user onboarding.
- **Bug Resolution:**
    - Fixed file-creation errors where newline characters () were being saved as literals.
    - Resolved a frontend crash by clearing the Vite cache, which was serving stale, broken files.

**Earlier issues found/mentioned but not fixed**
- **SendGrid Integration:** The user selected SendGrid for email notifications, but no work has started. This is planned for a future phase.

**Known issue recurrence from previous fork**
- **Issue recurrence in previous fork**: N/A
- **Recurrence count**: N/A
- **Status**: N/A

**Code Architecture**


**Key Technical Concepts**
- **Stack:** React, TypeScript, Vite, FastAPI, PostgreSQL
- **BaaS:** Supabase (Auth, Database, Storage)
- **Security:** Row-Level Security (RLS) in PostgreSQL, Signed URLs for private storage access.
- **UI:** TailwindCSS with Shadcn/UI.
- **Frontend State:** TanStack Query ().
- **PDF Handling:**  for client-side watermarking.

**key DB schema**
- : 
- : 
- : 
- : 
- : (New) 

**changes in tech stack**
- No changes. The primary architectural decision was to actively develop the FastAPI backend for business logic rather than keeping the application purely Supabase-client-driven.

**All files of reference**
- **Critical Migration File:**  (Needs to be applied).
- **Core Buggy Components:**
    -  (Logic for creating/promoting admins).
    -  (User signup flow).
- **Key Backend File:**  (Mounts all the new API routers).
- **Instructional Docs:**
    -  (MUST READ to fix database issues).
    -  (Context on schema fixes).

**key api endpoints**
- : Health check.
- : Endpoint for the frontend to send audit events.
- : Endpoint to generate secure, temporary URLs for files in Supabase Storage.

**Critical Info for New Agent**
1.  **APPLY THE MIGRATION FIRST:** The most critical action is to apply the database migration. The file  contains all necessary schema changes. Follow the guide in . This will fix the role column not found errors and is a prerequisite for fixing the auth flow.
2.  **FIX ADMIN CREATION:** The user already registered error is the next blocker. The logic in  must be fixed to handle promoting an existing user to an admin role, not just creating new users. This may require adding an  column to the  table via a new migration.
3.  **VERIFY THE ENTIRE AUTH FLOW:** After fixing the above, thoroughly test the complete user journey:
    *   Teacher signup.
    -   Admin creation (for both a new user and an existing teacher).
    *   Post-signup redirects (e.g., forcing subject selection).

**documents and test reports created in this job**
- 
- 
- 
- 

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** There's a critical error: user already registered when creating an Admin. The root cause is the system is trying to re-register a user instead of assigning a role.
2.  **Summary:** The Admin creation flow is flawed. It should separate authentication (creating a user once) from authorization (assigning a role). The fix requires checking if a user exists and then updating their role, not calling the signup function again. All issues related to this must be fixed for a stable system.

**Project Health Check:**
- **Broken:** The core user authentication, registration, and administration features are non-functional due to a combination of logical bugs in the code and a desynchronized database schema.

**3rd Party Integrations**
- **Supabase:** Used for PostgreSQL database, authentication, and file storage.
- **SendGrid:** Planned for email notifications (future task). Requires a User API Key.

**Testing status**
- **Testing agent used after significant changes:** NO
- **Troubleshoot agent used after agent stuck in loop:** YES, to diagnose a syntax error in a component.
- **Test files created:** None.
- **Known regressions:** The entire user registration and administration system is currently in a broken, regressed state.

**Credentials to test flow:**
No credentials exist. The signup and admin creation flows must be fixed to allow for user creation and testing.

**What agent forgot to execute**
- **Apply Database Migrations:** The agent created multiple SQL migration files to fix schema issues but never executed them against the database. This is the primary reason for the persistent column not found errors and the overall instability of the application.
- **Complete the  Fix:** After identifying that  needed to be changed to *promote* users, the agent noted a dependency (adding an  column to ) but ended the session before creating the necessary migration for it.</analysis>
